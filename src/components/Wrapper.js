export default ({ children }) => {
    return (
        <div style={{
            maxWidth: '1100px',
            margin: '0 auto',
            padding: '18px'
        }}>
            {children}
        </div>
    )
}